export class Login {
  constructor(public loginId?: number, public username?: string, public password?: string) {
    this.loginId = loginId;
    this.username = username;
    this.password = password;

  }
}
